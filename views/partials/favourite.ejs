<form action="/favourites" method="POST" class="mt-2">
  <input type="hidden" name="homeId" value="<%= home._id.toString() %>">
  <button
    type="submit"
    class="w-full flex items-center justify-center gap-2 px-4 py-2 bg-green-600 hover:bg-green-700 text-white text-sm font-semibold rounded-lg shadow-md transition-all duration-200 ease-in-out"
  >
    💚 Add to Favourite
  </button>
</form>
