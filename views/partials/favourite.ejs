<form action="/favourites" method="POST" class="mt-2">
  <input type="hidden" name="homeId" value="<%= home._id.toString() %>">
  <button
    type="submit"
     class="favorite-btn w-full flex items-center justify-center gap-2 px-4 py-2.5 rounded-xl shadow transition-all duration-200 transform hover:scale-105"
  >
    💚 Add to Favourite
  </button>
</form>
