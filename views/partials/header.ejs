 <header class="bg-white shadow-md">
      <div class="max-w-7xl mx-auto flex justify-between items-center p-4">
        <a href="/" class="text-2xl font-bold text-rose-500">Airbnb</a>
        <nav class="flex gap-4 items-center">

          <a href="/home-list" class="<%= currentPage === 'Home' ? 'bg-rose-500 text-white' : 'hover:bg-rose-600' %> px-4 py-2 rounded transition">Home</a>

          <a href="/host/host-home-list" class="<%= currentPage === 'host-home-list' ? 'bg-rose-500 text-white' : 'hover:bg-rose-600' %> px-4 py-2 rounded transition">
            Host Home
          </a>

          <a href="/host/add-home" class="<%= currentPage === '/host/add-home' ? 'bg-rose-500 text-white' : 'hover:bg-rose-600' %> px-4 py-2 rounded transition">
            Add Home
          </a>
          <a href="/booking" class="<%= currentPage === 'bookings' ? 'bg-rose-500 text-white' : 'hover:bg-rose-600' %> px-4 py-2 rounded transition">
            Bookings
          </a>

          <a href="/favourites" class="<%= currentPage === 'favourites' ? 'bg-rose-500 text-white' : 'hover:bg-rose-600' %> px-4 py-2 rounded transition">
            Favorites
          </a>
          <div class="w-8 h-8 bg-gray-300 rounded-full"></div>
        </nav>
      </div>
    </header>