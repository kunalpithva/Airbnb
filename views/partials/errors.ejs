<% if (errors.length>0) { %>
          <div class="flex items-center gap-2 bg-red-50 border border-red-200 text-red-600 text-sm px-3 py-2 rounded mb-2">
            <% errors.forEach(error => { %>
              <li><%= error %></li>
            <% })%>
          </div>
<% } %>