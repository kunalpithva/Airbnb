<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add Your Home – Airbnb</title>
    <link rel="stylesheet" href="/output.css" />
  </head>
  <body class="bg-gray-50 text-gray-800 font-sans min-h-screen flex flex-col">

    <!-- Header -->
    <%- include('../partials/header') %>

    <!-- Form Section -->
    <main class="flex-grow flex items-center justify-center px-4">
      <div class="w-full max-w-xl bg-white shadow-md rounded-lg p-8 mt-8">
        <h1 class="text-2xl font-semibold mb-6 text-center"><%= editing ? "Edit" : "Add" %> Your Home</h1>

        <form action="/host/<%= editing ? 'edit-home': 'add-home' %>" method="POST" class="space-y-6">

          <input type="hidden" name="_id" value="<%= editing ? home._id : '' %>"/>
          <!-- Home Name -->
          <div>
            <label for="houseName" class="block text-sm font-medium text-gray-700 mb-1">
              Home Name 
            </label>
            <input
              type="text"
              id="houseName"
              name="houseName"
              value="<%= editing ? home.houseName : '' %>"
              placeholder="e.g., Cozy Mountain Cabin"
              required
              class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-red-400 shadow-sm"
            />
          </div>

          <!-- Location -->
          <div>
            <label for="location" class="block text-sm font-medium text-gray-700 mb-1">
              Location
            </label>
            <input
              type="text"
              id="location"
              name="location"
              value="<%= editing ? home.location : '' %>"
              placeholder="City, Country"
              required
              class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-red-400 shadow-sm"
            />
          </div>

          <!-- Price -->
          <div>
            <label for="price" class="block text-sm font-medium text-gray-700 mb-1">
              Price per Night ($)
            </label>
            <input
              type="number"
              id="price"
              name="price"
              value="<%= editing ? home.price : '' %>"
              placeholder="100"
              min="0"
              required
              class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-red-400 shadow-sm"
            />
          </div>

          <!-- photoUrl URL -->
          <div class="mb-6">
  <label for="photoUrl" class="block text-sm font-medium text-gray-700 mb-1">
    Photo URL
  </label>

  <% if (editing && home.photoUrl) { %>
    <div class="mb-3">
      <img src="<%= home.photoUrl %>" alt="Current Home Photo" class="w-full h-48 object-cover rounded-md border" />
      <p class="text-xs text-gray-500 mt-1">Current photo preview</p>
    </div>
  <% } %>

  <input
    type="url"
    id="photoUrl"
    name="photoUrl"
    value="<%= editing && home.photoUrl ? home.photoUrl : '' %>"
    placeholder="https://example.com/image.jpg"
    required
    class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-red-400 shadow-sm"
  />
</div>


          <!-- Rating -->
          <div>
            <label for="rating" class="block text-sm font-medium text-gray-700 mb-1">
              Rating (0–5)
            </label>
            <input
              type="number"
              id="rating"
              name="rating"
              min="0"
              max="5"
              step="0.1"
              value="<%= editing ? home.rating : '' %>"
              placeholder="4.5"
              required
              class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-red-400 shadow-sm"
            />
          </div>

          <!-- Submit Button -->
          <button
            type="submit"
            class="w-full bg-red-500 hover:bg-red-600 text-white font-semibold py-2 px-4 rounded-md transition duration-150"
          >
            <%= editing ? "Update" : "Submit" %>
          </button>
        </form>
      </div>
    </main>

    <!-- Footer -->
    <footer class="bg-white border-t mt-10">
      <div class="max-w-7xl mx-auto p-4 text-center text-gray-500 text-sm">
        © <%= new Date().getFullYear() %> Airbnb Clone. All rights reserved.
      </div>
    </footer>

  </body>
</html>
