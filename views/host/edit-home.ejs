<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add Your Home – Airbnb</title>
    <link rel="stylesheet" href="/output.css" />
  </head>
  <body class="bg-gray-50 text-gray-800 font-sans min-h-screen flex flex-col">

    <!-- Header -->
    <%- include('../partials/header') %>

    <!-- Form Section -->
    <main class="flex-grow flex items-center justify-center px-4">
      <div class="w-full max-w-xl bg-white shadow-lg rounded-2xl p-8 mt-12 border border-gray-100">
        <h1 class="text-3xl font-bold mb-8 text-center text-rose-600 tracking-tight">
          <%= editing ? "Edit" : "Add" %> Your Home
        </h1>

        <form action="/host/<%= editing ? 'edit-home': 'add-home' %>" method="POST" class="space-y-6" enctype="multipart/form-data">

          <input type="hidden" name="_id" value="<%= home._id %>">

          <!-- Home Name -->
          <div>
            <label for="houseName" class="block text-sm font-medium text-gray-700 mb-1">
              Home Name
            </label>
            <input
              type="text"
              id="houseName"
              name="houseName"
              value="<%= editing ? home.houseName : '' %>"
              placeholder="e.g., Cozy Mountain Cabin"
              required
              class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-rose-400 shadow-sm"
            />
          </div>

          <!-- Location -->
          <div>
            <label for="location" class="block text-sm font-medium text-gray-700 mb-1">
              Location
            </label>
            <input
              type="text"
              id="location"
              name="location"
              value="<%= editing ? home.location : '' %>"
              placeholder="City, Country"
              required
              class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-rose-400 shadow-sm"
            />
          </div>

          <!-- Price -->
          <div>
            <label for="price" class="block text-sm font-medium text-gray-700 mb-1">
              Price per Night ($)
            </label>
            <input
              type="number"
              id="price"
              name="price"
              value="<%= editing ? home.price : '' %>"
              placeholder="100"
              min="0"
              required
              class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-rose-400 shadow-sm"
            />
          </div>

          <!-- Photo URL -->
          <!-- Photo Upload -->
<div>
  <label for="photo" class="block text-sm font-medium text-gray-700 mb-1">
    Upload Photo
  </label>

  <% if (editing && home.photoUrl) { %>
    <div class="mb-3">
      <img src="<%= home.photoUrl %>" alt="Current Home Photo"
        class="w-full h-48 object-cover rounded-lg border border-gray-200 shadow-sm" />
      <p class="text-xs text-gray-500 mt-2 italic text-center">Current photo preview</p>
    </div>
  <% } %>

  <input
    type="file"
    id="photo"
    name="photo"
    accept="image/*"
    class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-rose-400 shadow-sm file:mr-4 file:py-2 file:px-4 file:rounded-md file:border-0 file:text-sm file:font-semibold file:bg-rose-100 file:text-rose-700 hover:file:bg-rose-200"
  />
</div>


          <!-- Rating -->
          <div>
            <label for="rating" class="block text-sm font-medium text-gray-700 mb-1">
              Rating (0–5)
            </label>
            <input
              type="number"
              id="rating"
              name="rating"
              min="0"
              max="5"
              step="0.1"
              value="<%= editing ? home.rating : '' %>"
              placeholder="4.5"
              required
              class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-rose-400 shadow-sm"
            />
          </div>

          <!-- Description -->
          <div>
            <label for="description" class="block text-sm font-medium text-gray-700 mb-1">
              Description
            </label>
            <textarea
              id="description"
              name="description"
              rows="4"
              placeholder="Write a detailed description of your home..."
              value="<%= editing ? home.description : '' %>"
              required
              class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-rose-400 shadow-sm resize-none"
            ><%= editing && home.description ? home.description : '' %></textarea>
          </div>

          


          <!-- Submit Button -->
          <button
            type="submit"
            class="w-full bg-rose-500 hover:bg-rose-600 text-white font-semibold py-3 rounded-lg transition duration-200 shadow-md"
          >
            <%= editing ? "Update" : "Submit" %>
          </button>
        </form>
      </div>
    </main>

    <!-- Footer -->
    <footer class="bg-white border-t mt-12">
      <div class="max-w-7xl mx-auto p-4 text-center text-gray-500 text-sm">
        © <%= new Date().getFullYear() %> Airbnb Clone. All rights reserved.
      </div>
    </footer>

  </body>
</html>
