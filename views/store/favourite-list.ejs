<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Airbnb | Favorites</title>
    <link rel="stylesheet" href="output.css" />
  </head>
 <body class="flex flex-col min-h-screen bg-gray-50 text-gray-800 font-sans">
    <!-- Navbar -->
    <%- include('../partials/header') %>

    <!-- Hero Section -->
    <!-- <section class="bg-[url('https://source.unsplash.com/1600x500/?travel,vacation')] bg-cover bg-center h-64 flex items-center justify-center">
      <div class="bg-black bg-opacity-50 p-6 rounded text-white text-center">
        <h2 class="text-3xl md:text-4xl font-semibold">Favorites Page</h2>
        <p class="text-lg mt-2">Explore homes, cabins, and unique places to stay</p>
      </div>
    </section> -->

    <!-- Home Listings -->
    <main class="flex-grow max-w-7xl mx-auto px-4 py-10">
      <h3 class="text-2xl font-bold mb-6">My Favorite Homes</h3>

     <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% favorites.forEach(favorite => { %>
        <% if (favorite && favorite._id) { %>
          <div class="bg-white rounded-xl shadow p-4 hover:shadow-lg transition duration-200">
            <a href="/home/<%= favorite._id %>">
              <img
                src="<%= favorite.photoUrl %>"
                alt="<%= favorite.houseName %>"
                class="rounded-md h-40 w-full object-cover mb-2"
              />
              <h3 class="text-xl font-semibold"><%= favorite.houseName %></h3>
              <p class="text-gray-600">📍 <%= favorite.location %></p>
              <p class="text-gray-700">💰 $<%= favorite.price %> / night</p>
              <p class="text-yellow-500 text-sm">⭐ <%= favorite.rating %> / 5</p>
            </a>

            <form action="/favourites/delete/<%= favorite._id %>" method="POST" class="mt-3 text-center">
              <button
                type="submit"
                class="bg-red-600 hover:bg-red-700 text-white text-sm font-medium py-2 px-4 rounded-lg shadow transition duration-200"
              >
                ❌ Remove from Favorites
              </button>
            </form>
          </div>
        <% } %>
      <% }); %>
    </div>

    </main>

    <!-- Footer -->
   <footer class="bg-white border-t">
    <div class="max-w-7xl mx-auto p-4 text-center text-gray-500 text-sm">
      © <%= new Date().getFullYear() %> Airbnb Clone. All rights reserved.
    </div>
  </footer>
  </body>
</html>
